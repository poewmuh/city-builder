using VContainer;
using VContainer.Unity;

namespace CityBuilder.Installers.Application
{
    public class BootLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}
